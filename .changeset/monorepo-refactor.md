---
'styled-map-package': major
'styled-map-package-api': major
---

Restructure into npm workspaces monorepo with separate packages for the JS API (`styled-map-package-api`) and CLI (`styled-map-package`).
