---
'styled-map-package-api': patch
'smp-noto-glyphs': patch
---

Build with TypeScript 7. The emitted declarations are unchanged apart from
formatting, so there is no change to the public type surface.
